{
  "name": "witnessgrid",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "migrate": "pnpm --filter @witnessgrid/infra migrate",
    "migrate:dry": "pnpm --filter @witnessgrid/infra migrate:dry",
    "seed": "pnpm --filter @witnessgrid/infra seed",
    "dev": "pnpm --parallel --filter @witnessgrid/backend --filter @witnessgrid/web dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
